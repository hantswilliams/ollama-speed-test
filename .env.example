# Copy this file to `.env` and fill in real values.
# `.env` is gitignored; this `.env.example` is safe to commit.

# Hugging Face access token. Get one at https://huggingface.co/settings/tokens
# (a "read" token is enough). Used by services/privacy_filter/download.py
# to avoid anonymous rate limits.
HF_TOKEN=
